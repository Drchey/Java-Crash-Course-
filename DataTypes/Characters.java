package DataTypes;

public class Characters {
    // char data type is used to store a single character.character must be
    // surrounded by single quotes ''.
    char grade = 'B';

    // We can use ASCII to also display certain characters
    char newGrade = 45;
}
